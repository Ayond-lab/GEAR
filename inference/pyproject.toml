[project]
name = "gear-inference"
version = "0.1.0"
requires-python = ">=3.12"
description = "Development-only synthetic CV extraction service for GEAR"

[tool.pytest.ini_options]
testpaths = ["tests"]

