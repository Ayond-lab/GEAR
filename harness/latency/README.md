# Latency Harness

A8 records `gear-policy` p95 latency over at least 200 trials while `gear-inference` is under load.

Required evidence:

- Latency histogram.
- Trial count.
- Environment metadata.
- Inference load profile.

