apiVersion: v1
kind: Service
metadata:
  name: gear-fixture-store
  namespace: gear-system
  labels:
    app.kubernetes.io/name: gear-fixture-store
    app.kubernetes.io/part-of: gear
spec:
  selector:
    app.kubernetes.io/name: gear-fixture-store
  ports:
    - name: http
      port: 8080
      targetPort: http
