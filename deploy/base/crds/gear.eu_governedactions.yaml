---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: governedactions.gear.eu
spec:
  group: gear.eu
  names:
    kind: GovernedAction
    listKind: GovernedActionList
    plural: governedactions
    shortNames:
    - ga
    singular: governedaction
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: GovernedAction is a runtime request for an action under a specific
          mandate.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              abilityRef:
                type: string
              abilityVersion:
                type: string
              actionClass:
                type: string
              confidence:
                type: string
              dataClasses:
                items:
                  type: string
                type: array
              idempotencyKey:
                type: string
              mandateRef:
                type: string
              mandateVersion:
                type: integer
              payloadDigest:
                type: string
              subjectRef:
                type: string
              triggerRef:
                properties:
                  eventId:
                    type: string
                  id:
                    type: string
                  type:
                    type: string
                required:
                - eventId
                - id
                - type
                type: object
            required:
            - abilityRef
            - abilityVersion
            - actionClass
            - confidence
            - dataClasses
            - idempotencyKey
            - mandateRef
            - mandateVersion
            - payloadDigest
            - subjectRef
            - triggerRef
            type: object
          status:
            properties:
              auditRef:
                type: string
              decision:
                type: string
              effectRef:
                type: string
              executionState:
                type: string
              ruleFired:
                properties:
                  id:
                    type: string
                  version:
                    type: integer
                required:
                - id
                - version
                type: object
            required:
            - auditRef
            - decision
            - executionState
            - ruleFired
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
