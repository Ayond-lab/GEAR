---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: escalationitems.gear.eu
spec:
  group: gear.eu
  names:
    kind: EscalationItem
    listKind: EscalationItemList
    plural: escalationitems
    shortNames:
    - esc
    singular: escalationitem
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: |-
          EscalationItem is the human decision record for a reserved or uncertain
          action.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              actionRef:
                type: string
              approverSet:
                items:
                  type: string
                type: array
              createdAt:
                format: date-time
                type: string
              evidenceRefs:
                items:
                  type: string
                type: array
              reason:
                type: string
              ruleFired:
                properties:
                  id:
                    type: string
                  version:
                    type: integer
                required:
                - id
                - version
                type: object
            required:
            - actionRef
            - approverSet
            - createdAt
            - evidenceRefs
            - reason
            - ruleFired
            type: object
          status:
            properties:
              decidedAt:
                format: date-time
                type: string
              decidedBy:
                type: string
              decision:
                type: string
              reasonRef:
                type: string
            required:
            - decidedAt
            - decidedBy
            - decision
            - reasonRef
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
