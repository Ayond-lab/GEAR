apiVersion: v1
kind: Service
metadata:
  name: gear-triggers
  namespace: gear-system
  labels:
    app.kubernetes.io/name: gear-triggers
    app.kubernetes.io/part-of: gear
spec:
  selector:
    app.kubernetes.io/name: gear-triggers
  ports:
    - name: http
      port: 8080
      targetPort: http
