apiVersion: v1
kind: Service
metadata:
  name: gear-mandate
  namespace: gear-system
  labels:
    app.kubernetes.io/name: gear-mandate
    app.kubernetes.io/part-of: gear
spec:
  selector:
    app.kubernetes.io/name: gear-mandate
  ports:
    - name: http
      port: 8080
      targetPort: http
