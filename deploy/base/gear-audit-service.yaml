apiVersion: v1
kind: Service
metadata:
  name: gear-audit
  namespace: gear-system
  labels:
    app.kubernetes.io/name: gear-audit
    app.kubernetes.io/part-of: gear
spec:
  selector:
    app.kubernetes.io/name: gear-audit
  ports:
    - name: http
      port: 8080
      targetPort: http
